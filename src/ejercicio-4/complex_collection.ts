/* import {Complex} from "./complex";
import {EffectibleCollection} from "./effectible_collection";

export class ComplexCollection extends EffectibleCollection<Complex> {
  constructor(private readonly items: Complex[]) {
    super(items);
  }

  getEffectible() {
    return this.items;
  }
} */